function factorial(n:float) : float
{
    if n > 0 {
        n * factorial(n - 1)
    }
    else { 1.0 }
}

function main() : void
{
    let n : float = input_float();
    print_float(factorial(n));
}
